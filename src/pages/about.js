import React from 'react';
import Layout from '../components/layout';

const about = () => {
    return (
        <div>
            <Layout pageTitle='About Me'>
            <p>Hi there! Gatsby is pretty rad.</p>
            </Layout>
        </div>
    );
};

export default about;